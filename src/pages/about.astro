---
import Layout from "../layouts/Layout.astro";
---

<Layout title="about — gericapo98" current="about">
  <p><span class="prompt">gericapo98@site:~$</span> <span class="out">cat about.txt</span></p>
  <p>
    Focus: systems programming in Rust, Go, and C. Drawn to security
    tooling — supply-chain and dependency-integrity problems in
    particular. Currently learning robotics and simulation, working
    through simulated worlds toward one goal: a simulated robot that
    walks. Side project: a badminton self-coaching tool that tracks
    shuttle motion from video to catch technique errors a mirror won't.
  </p>

  <p><span class="prompt">gericapo98@site:~$</span> <span class="out">cat skills.json</span></p>
  <pre><code><span class="punc">&#123;</span>
  <span class="key">"languages"</span><span class="punc">:</span> <span class="punc">[</span><span class="str">"Rust"</span><span class="punc">,</span> <span class="str">"Go"</span><span class="punc">,</span> <span class="str">"C"</span><span class="punc">,</span> <span class="str">"Python"</span><span class="punc">]</span><span class="punc">,</span>
  <span class="key">"learning"</span><span class="punc">:</span> <span class="punc">[</span><span class="str">"Rust"</span><span class="punc">,</span> <span class="str">"Go"</span><span class="punc">,</span> <span class="str">"robotics &amp; simulation"</span><span class="punc">]</span><span class="punc">,</span>
  <span class="key">"tools"</span><span class="punc">:</span> <span class="punc">[</span><span class="str">"Git"</span><span class="punc">,</span> <span class="str">"SQLite"</span><span class="punc">,</span> <span class="str">"Linux"</span><span class="punc">,</span> <span class="str">"Newton (sim)"</span><span class="punc">]</span>
<span class="punc">&#125;</span></code></pre>
</Layout>
